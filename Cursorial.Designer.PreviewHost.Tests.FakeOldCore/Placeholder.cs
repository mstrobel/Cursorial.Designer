namespace Cursorial.Designer.Tests.FakeOldCore;

/// <summary>
/// Nothing here on purpose: the version gate reads assembly METADATA only, so the fixture's
/// entire job is done by its identity (<c>Cursorial.Core</c>, 0.1.0.0) in the csproj.
/// </summary>
internal static class Placeholder;
