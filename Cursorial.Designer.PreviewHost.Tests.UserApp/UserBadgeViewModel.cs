namespace Cursorial.Designer.Tests.UserApp;

/// <summary>Keeps the fixture app-shaped (real user outputs carry viewmodels, not just controls).</summary>
public sealed class UserBadgeViewModel
{
    public string Label { get; init; } = "from the user's viewmodel";
}
